# Environment Variables Template
# Copy this file to .env and fill in your actual values
# DO NOT commit the .env file to git

# ============================================
# GMAIL API CREDENTIALS (REQUIRED)
# ============================================
# 1. Go to: https://console.cloud.google.com/
# 2. Create a new project or select existing one
# 3. Enable Gmail API
# 4. Create OAuth 2.0 credentials (Desktop app)
# 5. Download credentials.json to this directory
# 6. First run will prompt for authentication and create token.json

# Gmail API Scopes Used:
# - https://www.googleapis.com/auth/gmail.modify

# ==========================================
# Anthropic API (for email categorization & invoice extraction)
# ==========================================
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# ==========================================
# OpenAI API (fallback for categorization & invoice extraction)
# ==========================================
OPENAI_API_KEY=your_openai_api_key_here

# ============================================
# INVOICE STORAGE SETTINGS
# ============================================
# Directory where invoices will be saved (default: invoices/)
INVOICE_DIR=invoices

# Directory for intermediate files (default: .tmp/)
TMP_DIR=.tmp

# ============================================
# GMAIL SEARCH SETTINGS
# ============================================
# Maximum results per search query (default: 10)
MAX_SEARCH_RESULTS=10

# Number of search queries to try per transaction (default: 2)
MAX_SEARCH_QUERIES=2

# ============================================
# OPTIONAL: OCR FOR SCANNED PDFs
# ============================================
# Set to true to enable OCR for scanned invoice PDFs (requires Tesseract installed)
USE_OCR=false
